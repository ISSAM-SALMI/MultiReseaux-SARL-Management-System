# CONFIGURATION PRODUCTION - Google Cloud
# Copiez ce fichier en .env.prod et remplissez avec vos vraies valeurs

# ===== SECURITE =====
# Générez une clé secrète unique pour la production
# Utilisez: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here-change-me

# DEBUG doit être à 0 en production
DEBUG=0

# ===== DOMAINES ET HOSTS =====
# IMPORTANT: Remplacez XX.XX.XX.XX par votre IP Google Cloud ou votre nom de domaine
# Format: Séparés par des espaces
ALLOWED_HOSTS=XX.XX.XX.XX localhost 127.0.0.1

# IMPORTANT: Ajoutez http:// ou https:// selon votre configuration
# Si vous utilisez HTTPS (recommandé), utilisez https://
# Format: Séparés par des espaces
CSRF_TRUSTED_ORIGINS=http://XX.XX.XX.XX http://localhost

# ===== BASE DE DONNÉES =====
# PostgreSQL en production
POSTGRES_DB=multisarl_db
POSTGRES_USER=multisarl_user
POSTGRES_PASSWORD=your-strong-password-here
POSTGRES_HOST=db
POSTGRES_PORT=5432

# URL complète de la base de données
DATABASE_URL=postgresql://multisarl_user:your-strong-password-here@db:5432/multisarl_db

# ===== REDIS =====
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# ===== STOCKAGE (Optionnel - pour Google Cloud Storage) =====
# Laissez à false pour utiliser le stockage local
USE_S3=false
# Si USE_S3=true, configurez ces valeurs:
# AWS_ACCESS_KEY_ID=your-gcs-access-key
# AWS_SECRET_ACCESS_KEY=your-gcs-secret-key
# AWS_STORAGE_BUCKET_NAME=your-bucket-name

# ===== CORS (Optionnel) =====
# En production, listez uniquement les origines autorisées
# CORS_ALLOWED_ORIGINS=http://XX.XX.XX.XX

# ===== EMAIL (Optionnel) =====
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password
